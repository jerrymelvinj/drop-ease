import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  weight: ["300", "400", "500", "600", "700"],
  subsets: ["latin"],
  variable: "--font-poppins",
  display: "swap",
});

export const metadata: Metadata = {
  title: "HR Resume Extractor & Candidate Intake",
  description: "Internal HR resume parser with human-in-the-loop review and Excel database export",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={poppins.variable}>
      <body className="font-poppins bg-pageBg min-h-screen text-gray-800 antialiased">
        {children}
      </body>
    </html>
  );
}
